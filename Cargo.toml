[package]
name = "plato-coordination"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Cross-room fleet coordination for PLATO nervous system"
repository = "https://github.com/SuperInstance/plato-coordination"

[dependencies]
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
serde_json = "1.0.150"
